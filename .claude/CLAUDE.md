@../AGENTS.md

# Claude Code

- Treat `../AGENTS.md` as the canonical repository guidance.
- Project skill wrappers live under `skills/` and delegate to `../.agents/skills/`.
- Edit the canonical `.agents/skills/` source rather than copying full skill content into `.claude/`.
